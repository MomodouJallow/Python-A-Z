# Using the range() function
for value in range(1, 5):
    print(value)  # Displays 1 through 4
    
# Making a numerical list using range()
numbers = list(range(1, 6))
print(numbers)

# A list of even numbers only
even_numbers = list(range(2, 11, 2))
print(even_numbers)